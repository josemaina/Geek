import 'package:flutter/material.dart';

class StaffHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('MedTrack Staff')),
      body: Center(child: Text('Welcome to Staff Dashboard!')),
    );
  }
}
